import Nat
import List

theorem thm: if (<T> all xs:List<T>. map(xs, fun x:T { x }) = xs)
  then map(node(ℕ1, empty), fun x:Nat { x }) = node(ℕ1, empty)
proof
  suppose prem: (<T> all xs:List<T>. map(xs, fun x:T { x }) = xs)
  prem
end
