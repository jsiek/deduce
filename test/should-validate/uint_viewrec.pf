import UInt
import List

fun keep(n:UInt) {
  n
}

assert keep(2) = 2

recursive replicate<T>(UInt, T) -> List<T> {
  replicate(zero, x) = []
  replicate(suc(p), x) = node(x, replicate(p, x))
}

print replicate(3, 42)
