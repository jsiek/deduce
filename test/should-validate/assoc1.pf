import Nat

theorem T: all x:Nat,y:Nat,z:Nat.
  x + suc(y) + z = suc(x + y + z)
proof
  arbitrary x:Nat,y:Nat,z:Nat
  equations
      x + suc(y) + z
      = suc(x + y) + z     by exchange add_suc.
  ... = suc(x + y + z)     by exchange suc_add.
end
