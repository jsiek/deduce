import Nat
import List

theorem length__nat_one: all x:Nat. length([x]) = 1
proof
  arbitrary x:Nat
  expand 2*length
  add_zero[1]
end


theorem length__nat_one': all x:Nat. length([x]) = 1
proof
  arbitrary x:Nat
  expand 2*length
  add_zero[1]
end
