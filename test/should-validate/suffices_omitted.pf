import UInt
import List

theorem length__nat_one: all x:UInt. length([x]) = 1
proof
  arbitrary x:UInt
  expand 2*length
  uint_add_zero[1]
end


theorem length__nat_one': all x:UInt. length([x]) = 1
proof
  arbitrary x:UInt
  expand 2*length
  uint_add_zero[1]
end
