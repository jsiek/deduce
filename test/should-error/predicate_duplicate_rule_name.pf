// Two rules within the same predicate share a name. Rule names
// become theorems after translation, so they can't be overloaded.

import UInt

predicate even : fn UInt -> bool {
  ev0 : even(0)
  ev0 : even(2)
}
