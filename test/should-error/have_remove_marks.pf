import List
import Nat

theorem blah : all x : Nat.
  length(node(x, empty)) = 1
proof
  arbitrary x : Nat
  have step : length(@empty<Nat>) + #length(@empty<Nat>)#
    = length(@empty<Nat>) + 0
    by expand length.
  ?
end
