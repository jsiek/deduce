theorem T: all P:bool, Q:bool, R:bool. if (if P then R) then (if Q then R)
proof
  arbitrary P:bool, Q:bool, R:bool
  suppose prem
  prem
end
theorem T: all P:bool, Q:bool, R:bool. if (if P then R) then (if Q then R)
proof
  arbitrary P:bool, Q:bool, R:bool
  suppose prem
  prem
end
