// Phase 2f (issue #1115): a straight-line `proc` with a discharged
// postcondition is now verified. `do_nothing` has an empty body and an
// `ensures x = x` that holds trivially, so the checker accepts it with no
// diagnostic -- the Phase 1m "accepted but not verified" warning (issue
// #1108) is retired for procedures this slice fully verifies. The absence of
// a `.pf.warn` sibling pins that this run is warning-free under both parsers.
proc do_nothing(x: bool)
  ensures x = x
{
}
