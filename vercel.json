{
	"version": 2,
	"installCommand": "pip install -r live_code_vercel_api/requirements.txt && mv -v lib /tmp/lib",
	"buildCommand": "cp -r live_code_vercel_api /vercel/output",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "live_code_vercel_api/api.py"
		}
	]
}
